# Model Configuration
MODEL_DIR=./models
MAX_BATCH=100

# Database
DB_PATH=./data/toxicity.db

# Redis (leave empty to disable caching)
REDIS_URL=redis://localhost:6379

# Server
HOST=0.0.0.0
PORT=8000
LOG_LEVEL=info

# Thresholds (JSON format)
THRESHOLDS={"default": 0.5, "ar": 0.40, "tr": 0.42, "ja": 0.48, "en": 0.50}
